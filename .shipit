host='igor-pi'
path='/usr/local/domiqtt/'

[deploy:local]
git push origin master

[deploy]
git checkout master
git pull
npm install
sudo bash -c "sudo pkill domiqtt || echo "
sudo bash -c "cd /usr/local/domiqtt/; npm start > /var/log/domiqtt.log 2>&1 &"

[status]
ps -ef | grep domiqtt
